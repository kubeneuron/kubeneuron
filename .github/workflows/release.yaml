name: Release

# Tag-driven release: multi-arch images to GHCR (cosign-signed, with SBOMs),
# a single-file install manifest, kubeneuronctl binaries, and checksums.
on:
  push:
    tags: ["v*"]

permissions:
  contents: write
  packages: write
  id-token: write # cosign keyless signing

env:
  IMAGE_REPO: ghcr.io/${{ github.repository_owner }}/kube-neuron
  KUSTOMIZE_VERSION: v5.6.0

jobs:
  release:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25.12"
          cache: true

      - uses: docker/setup-qemu-action@v4
      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: sigstore/cosign-installer@v3

      - uses: anchore/sbom-action/download-syft@v0
        id: syft

      - name: Build, push, sign, and SBOM the images
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          mkdir -p dist
          # published image name -> Dockerfile stage
          for entry in operator=operator controller=controller agent=agent kubeneuronctl=cli; do
            name="${entry%%=*}"
            stage="${entry#*=}"
            image="${IMAGE_REPO}/${name}"
            docker buildx build \
              --file build/Dockerfile \
              --platform linux/amd64,linux/arm64 \
              --target "$stage" \
              --build-arg VERSION="$version" \
              --tag "$image:$version" \
              --provenance=false \
              --push .
            digest="$(docker buildx imagetools inspect "$image:$version" \
              --format '{{json .Manifest}}' | jq -r .digest)"
            test -n "$digest" && [[ "$digest" == sha256:* ]]
            cosign sign --yes "$image@$digest"
            "${{ steps.syft.outputs.cmd }}" "$image:$version" \
              -o spdx-json > "dist/sbom-${name}-${version}.spdx.json"
            echo "${image}@${digest}" >> dist/images.txt
          done

      - name: Build single-file install manifest
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          go run "sigs.k8s.io/kustomize/kustomize/v5@${KUSTOMIZE_VERSION}" \
            version
          workdir="$(mktemp -d)"
          cp -r config "$workdir/config"
          (
            cd "$workdir/config/default"
            go run "sigs.k8s.io/kustomize/kustomize/v5@${KUSTOMIZE_VERSION}" \
              edit set image \
              "${IMAGE_REPO}/operator=${IMAGE_REPO}/operator:${version}"
          )
          go run "sigs.k8s.io/kustomize/kustomize/v5@${KUSTOMIZE_VERSION}" \
            build "$workdir/config/default" > "dist/kubeneuron-install-${version}.yaml"
          grep -q "image: ${IMAGE_REPO}/operator:${version}" \
            "dist/kubeneuron-install-${version}.yaml"
          cp deploy/install.sh "dist/install-${version}.sh"
          cp deploy/install.sh dist/install.sh

      - name: Build kubeneuronctl binaries
        shell: bash
        run: |
          set -euo pipefail
          version="${GITHUB_REF_NAME}"
          for platform in linux/amd64 linux/arm64 darwin/amd64 darwin/arm64; do
            goos="${platform%%/*}"; goarch="${platform#*/}"
            CGO_ENABLED=0 GOOS="$goos" GOARCH="$goarch" \
              go build -trimpath -ldflags "-s -w -X main.version=${version}" \
              -o "dist/kubeneuronctl-${version}-${goos}-${goarch}" ./cmd/kubeneuronctl
          done

      - name: Checksums
        shell: bash
        run: |
          set -euo pipefail
          cd dist
          sha256sum -- * > "../checksums-${GITHUB_REF_NAME}.txt"
          mv "../checksums-${GITHUB_REF_NAME}.txt" .

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          draft: true
          generate_release_notes: true
          files: dist/*
