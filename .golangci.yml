version: "2"

run:
  timeout: 5m

linters:
  # Standard set: errcheck, govet, ineffassign, staticcheck, unused.
  # Extend deliberately; every newly enabled linter must land green.
  default: standard
  enable:
    - misspell
    - unconvert
