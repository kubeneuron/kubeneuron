apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # install-no-webhook avoids making cert-manager an implicit dependency. The
  # release asset checksum and authoritative install procedure are locked in
  # ../versions.lock.yaml and ../README.md; use server-side apply for its CRDs.
  - https://github.com/VictoriaMetrics/operator/releases/download/v0.73.1/install-no-webhook.yaml
