# Forbidden documentation claims — one extended regex per line, '#' comments.
#
# Each entry is a status claim that WAS true once and went false; hack/
# verify-docs.sh greps every published markdown file and fails on any match,
# so a stale spelling can never rot silently back in (the failure mode the
# 2026-08-05 re-audit found in five audience-facing docs at once). When a
# status flips, add the old spelling here in the same commit.
#
# Keep patterns narrow enough not to forbid HISTORY sections: CHANGELOG.md is
# exempt in the checker.
executionMode:? .?Enabled.? (is|remains|stays) (deliberately )?(rejected|prohibited|locked)
operator rejects .?(executionMode:? )?.?Enabled
PostgreSQL.{0,60}(is not implemented|rejects (it|that store)|no controller PostgreSQL backend)
webhook.{0,30}does not (yet )?authenticate
does not yet authenticate Alertmanager
the (current )?skeleton
still an early skeleton
frozen at 2026-07-14
six CRDs
--version v0\.1\.[01]
unfinished (KubeNeuron )?runtime
preview profile
child-resource status is still

# --- vendor support claims -------------------------------------------------
# The second category in this file: claims that are not yet true. Shipping
# detection and actions are NVIDIA-only (docs/reference-capabilities.md is the
# per-vendor matrix, machine-checked against the filesystem by verify-docs.sh).
# These patterns fail the build on the prose spellings of shipped multi-vendor
# support, so a README or tour sentence cannot quietly outrun the matrix.
# When a vendor genuinely lands, delete the patterns it makes true IN THE SAME
# COMMIT as the matrix cells — that deletion is the deliberate act this file
# exists to force.
[Ss]upports? (both )?(NVIDIA and (AMD|Intel)|(AMD|Intel) and NVIDIA)
(AMD|Intel|ROCm)[^|]{0,40}(is|are) (now |fully |already )*(supported|hardware-validated)
[Mm]ulti-?vendor[^|]{0,20}(support|detection|remediation) (is|has) (shipped|landed|arrived|live)
(amd-smi|rocm-smi|amdgpu|xpu-smi|[Ll]evel ?[Zz]ero)[^|]{0,40}(ships|shipped|is (implemented|wired|supported))
# AMD detection ships (unvalidated) since the definition round; the
# overclaim to guard is now VALIDATION, not existence.
(AMD|Intel|ROCm)[^|]{0,40}(hardware[- ]validated|validated on (real |AMD |Intel )*hardware|proven on hardware)
# Vendor-support claims that go stale the moment a vendor adapter lands.
[Ss]hipping detection is NVIDIA-only
[Ss]hipping detection is NVIDIA[- ]only
